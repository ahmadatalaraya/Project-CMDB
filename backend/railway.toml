[build]
builder = "NIXPACKS"
buildCommand = "mvn -B clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar target/cmdb-0.0.1-SNAPSHOT.jar --spring.profiles.active=mysql"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
